# CI / local tests — everything except MLX (runs on any platform)
fastapi>=0.115
uvicorn>=0.30
requests>=2.32
psutil>=6.0
prometheus-client>=0.20
anyio>=4.4
pydantic>=2.8
httpx>=0.27          # fastapi TestClient
pytest>=8.0
ruff>=0.6

# Runtime (Mac mini)
mlx-lm[train]>=0.24   # training + OpenAI-compatible server; Apple Silicon only
fastapi>=0.115
uvicorn>=0.30
requests>=2.32
psutil>=6.0
prometheus-client>=0.20
anyio>=4.4
pydantic>=2.8
boto3>=1.34         # SSM Parameter Store secret resolution (see deploy/SECRETS.md)
